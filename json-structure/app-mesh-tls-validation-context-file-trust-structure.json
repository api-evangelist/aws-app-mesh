{
  "type": "object",
  "properties": {
    "certificateChain": {
      "allOf": [
        {
          "$ref": "#/components/schemas/FilePath"
        },
        {
          "description": "The certificate trust chain for a certificate stored on the file system of the virtual node that the proxy is running on."
        }
      ]
    }
  },
  "required": [
    "certificateChain"
  ],
  "description": "An object that represents a Transport Layer Security (TLS) validation context trust for a local file.",
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/aws-app-mesh/refs/heads/main/json-structure/app-mesh-tls-validation-context-file-trust-structure.json",
  "name": "TlsValidationContextFileTrust"
}